
  # Inventory Management Prototype

  This is a code bundle for Inventory Management Prototype. The original project is available at https://www.figma.com/design/A67FuBcL3WCKKQI8CdKppv/Inventory-Management-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  